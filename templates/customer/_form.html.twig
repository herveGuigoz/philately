{{ form_start(form) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>
    <div class="mt-6 grid grid-cols-4 gap-6">
        <div class="col-span-4 sm:col-span-2">
            {{ form_label(form.lastname, 'Nom', 
                {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
            }) }}
            {{ form_widget(form.lastname, 
                {'attr': {
                    'class': 'mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-gray-900 focus:border-gray-900 sm:text-sm',
                    'autocomplete': 'given-name',
                }})
            }}
        </div>

        <div class="col-span-4 sm:col-span-2">
            {{ form_label(form.firstname, 'Prénom', 
                {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
            }) }}
            {{ form_widget(form.firstname, 
                {'attr': {
                    'class': 'mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-gray-900 focus:border-gray-900 sm:text-sm',
                    'autocomplete': 'family-name',
                }})
            }}
        </div>

        <div class="col-span-4 sm:col-span-2">
            {{ form_label(form.pseudo, 'Pseudo*',
                {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
            }) }}
            {{ form_widget(form.pseudo, 
                {'attr': {'class': 'mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-gray-900 focus:border-gray-900 sm:text-sm',
                }})
            }}
        </div>

        <div class="col-span-4 sm:col-span-2">
            {{ form_label(form.adress, 'Address', 
                {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
            }) }}
            {{ form_widget(form.adress, 
                {'attr': {'class': 'mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-gray-900 focus:border-gray-900 sm:text-sm'}})
            }}
        </div>

        <div class="col-span-4 sm:col-span-2">
            {{ form_label(form.city, 'Ville', 
                {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
            }) }}
            {{ form_widget(form.city, 
                {'attr': {'class': 'mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-gray-900 focus:border-gray-900 sm:text-sm'}})
            }}
        </div>

        <div class="col-span-4 sm:col-span-2">
            {{ form_label(form.postCode, 'Code Postal', 
                {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}
            }) }}
            {{ form_widget(form.postCode, 
                {'attr': {'class': 'mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-gray-900 focus:border-gray-900 sm:text-sm'}})

            }}
        </div>
    </div>

    <div class="py-4 text-left text-xs text-gray-400">
        *Required
    </div>
    <div class="py-4 flex justify-between">
        <a href="{{ path('customer_index') }}" class="btn-medium btn-gray">
            Retour
        </a>

        <button type="submit" class="btn-medium btn-black">
            {{ button_label|default('Save') }}
        </button>
    </div>
{{ form_end(form) }}